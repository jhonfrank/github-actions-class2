name: Deploy to server

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    
    env:
      MESSAGE: Hola mundo

    steps:
      - name: Working dir
        run: echo $GITHUB_OUTPUT
        
  second-job:
    needs: first-job
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo
        run: echo "Hola mundo segundo"